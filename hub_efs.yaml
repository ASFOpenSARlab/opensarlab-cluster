
apiVersion: v1
kind: Namespace
metadata:
  name: jupyter

---

apiVersion: storage.k8s.io/v1
kind: StorageClass
provisioner: www.example.com/hub
metadata:
  name: hub-efs
parameters:
  text: "Hello"
allowVolumeExpansion: true

---

kind: PersistentVolume
apiVersion: v1
metadata:
  name: hub-efs
spec:
  capacity:
    storage: 42Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: hub-efs
  nfs:
    server: fs-c0386723.efs.us-east-1.amazonaws.com
    path: "/pods"

---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hub-efs
  namespace: jupyter
spec:
  storageClassName: hub-efs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 11Gi
