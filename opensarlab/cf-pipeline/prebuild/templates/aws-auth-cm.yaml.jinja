---

apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
data:
  mapRoles: |
    - rolearn: arn:aws:iam::{{ account_id }}:role/{{ region_name }}-{{ cost_tag_value }}-cluster-user-access
      username: user-access-role
      groups:
        - system:masters
    - rolearn: arn:aws:iam::{{ account_id }}:role/{{ region_name }}-{{ cost_tag_value }}-cluster-run-role
      username: cluster-run-role
      groups:
        - system:masters
    {% for node in opensarlab['nodes'] -%}
    {%- set node_name_escaped = node.name | regex_replace ("[^A-Za-z0-9]","00") -%}
    - rolearn: arn:aws:iam::{{ account_id }}:role/{{ region_name }}-{{ cost_tag_value }}-{{ node_name_escaped }}-instance-role
      username: system:node:{% raw %}{{EC2PrivateDNSName}}{% endraw %}
      groups:
        - system:bootstrappers
        - system:nodes
    {% endfor -%}
